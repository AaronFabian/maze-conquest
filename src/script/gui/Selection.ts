import { CanvasRendering } from '@/script/interface/state/CanvasRendering';

export class Selection implements CanvasRendering {
	constructor(x: number, y: number, width: number, height: number) {}

	update() {}

	render() {}
}
